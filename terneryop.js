// Ternery Operator
// Conditional operator is a shorthand for an if-else statement. It allows us to
// evaluate a value based on the condition being true or false. The syntax of ternary
// operator is: (condition) ? expr1 : expr2;
var marks=40;
var passed=marks>=40 ? 1:0;
console.log(passed)